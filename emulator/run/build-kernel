#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ $# -lt 2 ]]; then
    echo "Usage: build-kernel <target> <source.c|source.s|source.S> [options]" >&2
    exit 1
fi

exec python3 "${SCRIPT_DIR}/build-workload" "$@"
